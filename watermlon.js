// create a function that sees if two friends can adequately divide a watermelon between them. it has to return true if the value is even and more than 2, but must return false if that condition is not matchMedia.apply.apply. doesn't mean they still can't eat the watermelon.


function divide(weight){
    if (weight % 2 === 0 && weight > 2){
      return true
    }else{
      return false
    }
  }