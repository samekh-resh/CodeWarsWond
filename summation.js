let summation = function (num) {
    let resultingNumber = 0
    for (let i = 1; i <= num; i++){
       resultingNumber += i;
    }
    return resultingNumber
  }

  //purpose of this was to create a function that gave you a sequential sum of numbers, based on the number given
  //so if someone was given the number they would want the summation of 1 + 2 + 3 + 4 + 5 
  
  